import 'package:flutter/material.dart';

class AppColors {
  // Ana Renkler (Neon)
  static const Color neonCyan = Color(0xFF00F0FF);    // Blok ve UI parlamaları
  static const Color neonMagenta = Color(0xFFFF00AA); // İkincil vurgular
  static const Color neonGreen = Color(0xFF00FF41);   // Başarı/Skor artışı

  // Arka Planlar
  static const Color backgroundDark = Color(0xFF0A0A12); // Derin Siyah (Void)
  static const Color surfaceDark = Color(0xFF161625);    // Kartlar/Paneller
  
  // Glassmorphism (Cam Efekti) için
  static const Color glassWhiteLow = Color(0x0DFFFFFF);  // %5 Opaklık
  static const Color glassWhiteMedium = Color(0x1AFFFFFF); // %10 Opaklık
  
  // Grid Çizgileri
  static const Color gridLine = Color(0xFF252540);
}